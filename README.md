# PaceVer-web

This repository is the public GitHub Pages site for the PaceVer doctoral research project.

## Local preview

From the repository root:

```bash
python -m http.server 8000
```

Then open `http://127.0.0.1:8000`.

## Deployment

The repository includes a GitHub Actions workflow that publishes the repository root
to GitHub Pages.
